# ========================================
# FICHIER .env POUR LE SERVEUR DE PRODUCTION
# ========================================
# 
# Instructions :
# 1. Copiez tout le contenu de ce fichier
# 2. Créez un fichier ".env" à la racine de votre projet sur le serveur
# 3. Collez le contenu
# 4. Modifiez les valeurs selon votre configuration
# 5. Exécutez : php artisan key:generate
# 
# ========================================

APP_NAME=Domini
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://votredomaine.com

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# ========================================
# BASE DE DONNÉES
# ========================================
# À MODIFIER selon votre hébergeur
# Exemples :
# - Hostinger : DB_HOST=localhost
# - OVH : DB_HOST=mysql51-XX.pro
# - O2Switch : DB_HOST=localhost
# ========================================

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=votre_nom_de_base_de_donnees
DB_USERNAME=votre_user_mysql
DB_PASSWORD=votre_password_mysql

# ========================================
# CACHE ET SESSIONS
# ========================================

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=7200

# ========================================
# EMAIL (Optionnel - à configurer plus tard)
# ========================================

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@domini.com"
MAIL_FROM_NAME="${APP_NAME}"

# ========================================
# REDIS (Si disponible sur votre serveur)
# ========================================

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ========================================
# AUTRES SERVICES (Laisser tel quel)
# ========================================

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
